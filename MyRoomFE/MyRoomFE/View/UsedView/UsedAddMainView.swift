//
//  UsedAddMainView.swift
//  MyRoomFE
//
//  Created by 이수정 on 12/3/24.
//

import SwiftUI

struct UsedAddMainView: View {
//    @State private var isComplete: Bool = false
//    @State private var newUsedData:Used
//    @State private var newUsedPhotos:[UsedPhotoData]
    
    var body: some View {
//        VStack {
//                   if isComplete, let item = newUsedData {
//                       UsedDetailView(isFavorite: .constant(false), used: newUsedData, photos: newUsedPhotos)
//                           .transition(.move(edge: .trailing)) // 오른쪽으로 애니메이션
//                   } else {
//                       UsedAddView(onRegister: { item in
//                           self.newUsedData = item
////                           self.newUsedPhotos = item.image
//                           withAnimation(.easeInOut(duration: 0.5)) {
//                               self.isComplete.toggle()
//                           }
//                       })
//                   }
//               }
//               .animation(.easeInOut(duration: 0.5), value: isComplete)
        Text("text 중")
    }
}

#Preview {
    UsedAddMainView()
}
